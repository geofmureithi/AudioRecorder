//Automatically generated by rifgen
use crate::*;
use jni_sys::*;
foreign_class!(
	# [doc = " Sine-wave generator stream"]
	class AudioSineWaveGen {
		self_type AudioSineWaveGen;
		constructor AudioSineWaveGen::new()->AudioSineWaveGen;
		# [doc = " Create and start audio stream"]
		fn AudioSineWaveGen::try_start(& mut self); alias tryStart;
		# [doc = " Pause audio stream"]
		fn AudioSineWaveGen::try_pause(& mut self); alias tryPause;
		# [doc = " Stop and remove audio stream"]
		fn AudioSineWaveGen::try_stop(& mut self); alias tryStop;
		# [doc = " Print device's audio info"]
		fn AudioSineWaveGen::audio_probe(); alias audioProbe;
	}
);
